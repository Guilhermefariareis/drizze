# Supabase configuration (use either the VITE_* or non-VITE variants)
SUPABASE_URL="https://your-project-id.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Optional VITE-prefixed variants (if you already use them in the app)
VITE_SUPABASE_URL="https://your-project-id.supabase.co"
VITE_SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Notes:
# - SERVICE ROLE KEY is sensitive; keep it private and never expose in frontend.
# - The scripts use SUPABASE